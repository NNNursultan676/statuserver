# 🚀 Быстрый старт Status Server

## Для локального запуска в Docker

### Шаг 1: Настройка переменных окружения

```bash
cd statuserver
cp .env.example .env
```

Отредактируйте `.env`:

```env
# URL вашего Monitoring API
METRICS_API_URL=http://10.183.45.198:8000

# Или если через туннель:
# METRICS_API_URL=http://localhost:8000

# Пароль администратора (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here
```

### Шаг 2: Запуск Docker

```bash
# Сборка и запуск
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f
```

### Шаг 3: Проверка

Откройте в браузере: **http://localhost:5000**

Вы должны увидеть:
- ✅ Dashboard с вашими серверами
- ✅ Актуальные метрики CPU/RAM/Disk
- ✅ Статусы в реальном времени

## 📊 Что произойдет

1. **При первом запуске:**
   - Приложение подключится к `http://10.183.45.198:8000`
   - Автоматически загрузит все 13 серверов из Monitoring API
   - Создаст SQLite базу данных в `data/services.db`
   - Определит категории и статусы автоматически

2. **При работе:**
   - Каждый запрос к `/api/services` синхронизирует данные с API
   - Метрики обновляются в реальном времени
   - История изменений сохраняется в БД

3. **Если API недоступен:**
   - Приложение продолжит работать с сохраненными данными
   - При восстановлении связи - автоматически синхронизируется

## 🌐 Настройка туннеля (если API за VPN)

### Вариант 1: SSH Туннель

```bash
ssh -L 8000:10.183.45.198:8000 user@vpn-server -N

# В .env укажите:
METRICS_API_URL=http://localhost:8000
```

### Вариант 2: Host Network (Docker)

В `docker-compose.yml`:

```yaml
services:
  app:
    network_mode: "host"
    environment:
      - METRICS_API_URL=http://localhost:8000
```

## 📁 Структура проекта

```
statuserver/
├── client/                  # React frontend
│   └── src/
│       ├── pages/          # Страницы (Dashboard, Analytics, etc)
│       └── components/      # UI компоненты
├── server_py/              # Python backend
│   ├── main.py            # Точка входа FastAPI
│   ├── routes.py          # API endpoints
│   ├── metrics_api_client.py  # Клиент для Monitoring API
│   ├── storage.py         # In-memory хранилище
│   └── db_storage.py      # SQLite постоянное хранилище
├── data/                   # База данных (создается автоматически)
│   └── services.db
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile             # Docker образ
└── .env                   # Ваши настройки
```

## 🔑 API Endpoints

### Публичные:
- `GET /` - Главная страница
- `GET /api/services` - Список всех сервисов + синхронизация с Monitoring API
- `GET /api/services/{id}` - Детали сервиса
- `GET /api/incidents` - Список инцидентов
- `GET /api/metrics-api/status` - Статус подключения к Monitoring API

### Админ (требует авторизации):
- `POST /api/admin/auth` - Вход в админ-панель
- `POST /api/services` - Создание сервиса вручную
- `PATCH /api/services/{id}/status` - Обновление статуса
- `POST /api/import` - Импорт сервисов из JSON
- `POST /api/incidents` - Создание инцидента

## 🛠️ Разработка в Replit

Приложение **уже работает** в Replit:

```bash
# Workflow автоматически запускает:
cd statuserver
npx vite &                # Frontend на :5173
python server_py/main.py  # Backend на :5000
```

Просто откройте Preview в Replit!

## 📚 Документация

- **README_DOCKER.md** - Полная документация Docker setup
- **METRICS_API_INTEGRATION.md** - Детальное описание интеграции с Monitoring API
- **QUICKSTART.md** - Этот файл

## 🎯 Следующие шаги

1. **Скачайте проект:**
   ```bash
   # В Replit Shell
   cd /home/runner/workspace
   zip -r statuserver.zip statuserver/
   # Скачайте statuserver.zip
   ```

2. **Разверните локально:**
   ```bash
   unzip statuserver.zip
   cd statuserver
   cp .env.example .env
   nano .env  # Настройте
   docker-compose up --build
   ```

3. **Откройте в браузере:**
   - Dashboard: `http://localhost:5000`
   - Admin: `http://localhost:5000` → вкладка "Admin"

## ❓ FAQ

**Q: Как импортировать список сервисов из файла?**

A: Используйте скрипт `import_services.py`:
```bash
python3 import_services.py services.txt
```

**Q: Можно ли использовать без Monitoring API?**

A: Да! Просто не указывайте `METRICS_API_URL` или оставьте пустым. Добавляйте сервисы через Admin-панель.

**Q: Как настроить Grafana интеграцию?**

A: Добавьте в `.env`:
```env
GRAFANA_URL=http://your-grafana:3000
GRAFANA_API_TOKEN=your_token
```

**Q: Где хранятся данные?**

A: SQLite база в `data/services.db`. Данные сохраняются между перезапусками через Docker volume.

## 🎉 Готово!

Ваш Status Server готов к работе с интеграцией Monitoring API!

При запуске вы увидите все 13 серверов из вашей инфраструктуры:
- SIEM Server
- OPS Server
- AI Test Environment
- IPSec Server
- Stage Database
- Production Database
- Wazuh Demo
- Firezone VPN
- GitLab VM
- SSO Server
- Central Proxy
- AI Project VM
- Demo DB CreditBroker
