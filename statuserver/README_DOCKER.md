# Status Server - Docker Setup

Полное руководство по запуску Status Server в Docker-контейнере.

## 🚀 Быстрый старт

### 1. Импорт списка сервисов

Если у вас есть текстовый файл со списком сервисов:

```bash
# Скопируйте файл в папку statuserver
cp /path/to/your/services.txt statuserver/

# Импортируйте сервисы (опционально, если не используете внешний API)
python3 import_services.py services.txt
```

### 2. Запуск в Docker

```bash
cd statuserver

# Сборка и запуск
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

Приложение будет доступно по адресу: **http://localhost:5000**

## 📝 Конфигурация

### Переменные окружения

Создайте файл `.env` в папке `statuserver`:

```env
# Основные настройки
NODE_ENV=production
PORT=5000

# API метрик (опционально)
METRICS_API_URL=http://your-metrics-api:8000

# Grafana интеграция (опционально)
GRAFANA_URL=http://your-grafana:3000
GRAFANA_API_TOKEN=your_grafana_token

# Админ-панель (измените пароль!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password
```

⚠️ **Важно**: Обязательно измените пароль администратора перед деплоем в production!

### Docker Compose с переменными окружения

```yaml
version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/metrics-api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

## 🔌 Интеграция с Metrics API

Status Server может автоматически получать данные о статусе сервисов из внешнего Metrics API.

### Требуемые эндпоинты

Ваш Metrics API должен предоставлять следующие эндпоинты:

1. **Проверка доступности**
   ```
   GET /metrics/available
   Response: 200 OK
   ```

2. **Список серверов**
   ```
   GET /metrics/servers
   Response: [
     {
       "name": "SSO Server",
       "instance": "192.168.1.10:8080",
       "job": "Authentication",
       "region": "Production",
       "type": "Keycloak"
     }
   ]
   ```

3. **Метрики сервера**
   ```
   GET /metrics/servers/{server_name}
   Response: {
     "up": 1.0,  // 1 = работает, 0 = не работает
     "cpu": 45.2,
     "memory": 72.1
   }
   ```

4. **CPU метрики** (опционально)
   ```
   GET /metrics/servers/{server_name}/cpu
   Response: { "usage": 45.2 }
   ```

5. **Memory метрики** (опционально)
   ```
   GET /metrics/servers/{server_name}/memory
   Response: { "usage": 72.1 }
   ```

### Настройка туннеля к Metrics API

Если ваш Metrics API работает через VPN/туннель:

```bash
# Пример с SSH туннелем
ssh -L 8000:localhost:8000 user@your-server

# В .env файле
METRICS_API_URL=http://localhost:8000
```

## 🏗️ Архитектура

```
┌─────────────────┐
│   Frontend      │
│  (React+Vite)   │ ──┐
└─────────────────┘   │
                      │
┌─────────────────┐   │
│   Backend       │ ──┤ Docker Container
│ (Python FastAPI)│   │ Port 5000
└─────────────────┘   │
         │            │
         ▼            │
┌─────────────────┐   │
│  Metrics API    │ ──┘ (Внешний, опционально)
│   (Optional)    │
└─────────────────┘
```

## 📊 Использование без Metrics API

Если у вас нет внешнего Metrics API, приложение будет использовать локальное хранилище:

1. Импортируйте сервисы через скрипт:
   ```bash
   python3 import_services.py services.txt
   ```

2. Вручную управляйте статусами через Admin-панель:
   - Откройте http://localhost:5000
   - Перейдите в раздел "Admin"
   - Войдите с учетными данными из `.env`

## 🔧 Разработка

### Локальная разработка без Docker

```bash
# Установка зависимостей
npm install
pip install -r requirements.txt

# Запуск dev-сервера
./start.sh
```

### Обновление после изменений

```bash
# Пересборка образа
docker-compose down
docker-compose build --no-cache
docker-compose up
```

## 📦 Деплой в production

```bash
# Сборка образа
docker build -t status-server:latest .

# Запуск с production настройками
docker run -d \
  -p 5000:5000 \
  -e NODE_ENV=production \
  -e METRICS_API_URL=http://your-api:8000 \
  --restart unless-stopped \
  --name status-server \
  status-server:latest
```

## 🐛 Отладка

### Просмотр логов

```bash
# Логи в реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи только backend
docker-compose logs app
```

### Проверка health check

```bash
curl http://localhost:5000/api/metrics-api/status
```

### Подключение к контейнеру

```bash
docker-compose exec app bash
```

## 🔒 Безопасность

1. **Измените пароль администратора** в `.env`
2. **Используйте HTTPS** в production (настройте reverse proxy)
3. **Ограничьте доступ** к админ-панели через firewall
4. **Регулярно обновляйте** зависимости

## 📚 Дополнительные ресурсы

- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [Документация React](https://react.dev/)
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/)

## 💡 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что порт 5000 свободен
3. Проверьте доступность Metrics API (если используется)
