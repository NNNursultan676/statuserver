version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      # Раскомментируйте для PostgreSQL
      # - DATABASE_URL=postgresql://postgres:password@db:5432/statuserver
      # Раскомментируйте для Grafana
      # - GRAFANA_URL=http://grafana:3000
      # - GRAFANA_API_KEY=your-api-key
    # depends_on:
    #   - db
    restart: unless-stopped
    networks:
      - statuserver-network

  # Опционально: PostgreSQL база данных
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     - POSTGRES_DB=statuserver
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - statuserver-network
  #   restart: unless-stopped

  # Опционально: Grafana для мониторинга
  # grafana:
  #   image: grafana/grafana:latest
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   networks:
  #     - statuserver-network
  #   restart: unless-stopped

networks:
  statuserver-network:
    driver: bridge

# volumes:
#   postgres-data:
#   grafana-data:
