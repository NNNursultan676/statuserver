# Status Server - Система мониторинга сервисов

Платформа для мониторинга статуса сервисов с интеграцией внешнего Metrics API, аналитикой и управлением инцидентами.

## 🚀 Быстрый старт

### Локальный запуск (Replit)

Приложение уже настроено и запущено! Откройте его в браузере.

**Важно**: Приложение будет работать с локальным хранилищем, если Metrics API недоступен.

### Настройка Metrics API

Чтобы приложение получало данные из вашего Metrics API:

1. Откройте **Secrets** в Replit
2. Добавьте переменную окружения:
   ```
   METRICS_API_URL=http://your-metrics-api:8000
   ```
3. Перезапустите приложение (нажмите Stop → Run)

## 🔐 Доступ к админке

Для доступа к админским функциям используйте:

```
Логин: root
Пароль: 72416810
```

**Защищенные endpoints**:
- Создание сервисов
- Обновление статусов
- Создание инцидентов
- Импорт данных
- Синхронизация с Grafana

## 📋 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# Обязательно для получения данных из Metrics API
METRICS_API_URL=http://localhost:8000

# Опционально: Grafana интеграция
GRAFANA_URL=https://your-grafana-instance.com
GRAFANA_API_TOKEN=your-api-token-here

# Режим окружения
NODE_ENV=development

# Порт приложения
PORT=5000
```

## 🐳 Запуск через Docker

### Сборка образа

```bash
docker-compose build
```

### Запуск приложения

```bash
# С переменными окружения из .env файла
docker-compose up

# Или напрямую
docker-compose up -d
```

### Просмотр логов

```bash
docker-compose logs -f
```

### Остановка

```bash
docker-compose down
```

## 📡 API Endpoints

### Сервисы

- `GET /api/services` - Получить все сервисы
- `GET /api/services/{id}` - Получить конкретный сервис
- `POST /api/services` - Создать сервис (требуется авторизация)
- `PATCH /api/services/{id}/status` - Обновить статус (требуется авторизация)

### Инциденты

- `GET /api/incidents` - Получить все инциденты
- `POST /api/incidents` - Создать инцидент (требуется авторизация)

### Метрики

- `GET /api/server-metrics` - Получить метрики серверов
- `GET /api/status-history/{serviceId}` - История статусов сервиса

### Интеграция с Metrics API

- `GET /api/metrics-api/status` - Проверить доступность Metrics API

### Импорт/Экспорт

- `POST /api/import-services` - Импорт сервисов (требуется авторизация)
- `GET /api/export-services?format=json|csv` - Экспорт сервисов

## 🔌 Интеграция с вашим Metrics API

Приложение автоматически получает данные из вашего Metrics API используя следующие endpoints:

```
GET /metrics/available          - Проверка доступности
GET /metrics/servers             - Список всех серверов
GET /metrics/servers/{name}      - Метрики конкретного сервера
GET /metrics/servers/{name}/cpu  - CPU usage
GET /metrics/servers/{name}/memory - Memory usage
```

### Формат данных от Metrics API

Ожидаемый формат ответа от `/metrics/servers`:

```json
[
  {
    "name": "server-1",
    "instance": "192.168.1.10:9100",
    "job": "node_exporter",
    "category": "Backend",
    "region": "Production",
    "description": "Production server"
  }
]
```

## 🎯 Логика работы

1. **При запуске**: Приложение проверяет доступность Metrics API
2. **Если API доступен**: 
   - Получает список серверов
   - Получает метрики для каждого сервера
   - Определяет статус (operational/down/degraded)
   - Синхронизирует с локальным хранилищем
3. **Если API недоступен**:
   - Использует данные из локального хранилища
   - Показывает сообщение "Metrics API недоступен"

## 🛠 Разработка

### Установка зависимостей

```bash
# Node.js зависимости
npm install

# Python зависимости (уже установлены через Replit)
pip install fastapi httpx pydantic python-multipart uvicorn
```

### Запуск в режиме разработки

```bash
bash start.sh
```

Это запустит:
- **Vite dev server** на порту 5173 (frontend с HMR)
- **FastAPI server** на порту 5000 (backend API + proxy)

### Сборка для production

```bash
npm run build
```

Это создаст:
- `dist/public/` - собранный frontend
- `dist/server/` - собранный backend (если используется Node.js версия)

## 📊 Структура проекта

```
.
├── client/              # React frontend
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   ├── pages/       # Страницы (Dashboard, Analytics, Admin)
│   │   ├── hooks/       # React хуки
│   │   └── lib/         # Утилиты
│   └── public/          # Статические файлы
├── server_py/           # Python backend (FastAPI)
│   ├── main.py          # Точка входа
│   ├── routes.py        # API маршруты
│   ├── storage.py       # In-memory хранилище
│   ├── models.py        # Pydantic модели
│   ├── auth.py          # Аутентификация
│   ├── metrics_api_client.py  # Клиент для Metrics API
│   └── grafana_service.py     # Grafana интеграция
├── shared/              # Общие типы (TypeScript)
├── Dockerfile           # Docker конфигурация
├── docker-compose.yml   # Docker Compose
└── start.sh             # Скрипт запуска для разработки
```

## 🔄 Обработка недоступности API

Когда Metrics API недоступен:

1. **Backend**: Возвращает статус 503 с сообщением
2. **Frontend**: Должен показывать уведомление пользователю
3. **Локальное хранилище**: Используются последние известные данные

## 📝 Смена пароля администратора

Чтобы изменить пароль для админки:

1. Откройте файл `server_py/auth.py`
2. Измените значения:
   ```python
   ADMIN_USERNAME = "root"
   ADMIN_PASSWORD = "72416810"
   ```
3. Перезапустите приложение

## 🐛 Отладка

### Проверка логов

```bash
# Replit
# Логи отображаются в консоли автоматически

# Docker
docker-compose logs -f app
```

### Проверка доступности Metrics API

```bash
curl http://localhost:5000/api/metrics-api/status
```

### Тестирование аутентификации

```bash
# Без авторизации (должна быть ошибка 401)
curl -X POST http://localhost:5000/api/services \
  -H "Content-Type: application/json" \
  -d '{"name":"test","category":"test","region":"test"}'

# С авторизацией
curl -X POST http://localhost:5000/api/services \
  -u root:72416810 \
  -H "Content-Type: application/json" \
  -d '{"name":"test","category":"test","region":"test"}'
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Логи приложения
2. Доступность Metrics API
3. Переменные окружения
4. Правильность учетных данных для админки
